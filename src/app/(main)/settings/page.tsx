"use client";
import AppShell from "@/components/layout/AppShell";
export default function SettingsPage() { return <AppShell><h2 className="text-3xl font-bold">Settings</h2><p className="mt-3 text-gray-400">Configure your Supabase credentials and private access code in <code>.env.local</code>. Product categories are managed from the product form.</p></AppShell>; }
